#include <stdio.h>

/**
 * main - prints "Programming is like building a multilingual puzzle using puts
 * return: your program shouls end with the value 0
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
